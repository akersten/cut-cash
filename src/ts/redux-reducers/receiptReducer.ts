import {Receipt} from "../core/receipt/receipt";
/**
 * Created by akersten on 5/27/17.
 *
 * Reducer for a receipt object.
 */

const initialState = {
    receipt: new Receipt("Untitled", new Date()),
};




function receiptReducer(state = initialState, action) {
    switch (action.type) {
        case
    }
}